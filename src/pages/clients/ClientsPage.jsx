import ClientsList from './ClientsList';

export default function ClientsPage() {
  return (
    <div style={{ 
      padding: '2rem',
      paddingLeft: '2rem', 
      maxWidth: '100%'
    }}>
      <ClientsList />
    </div>
  );
}